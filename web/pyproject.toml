[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kb-chatbot-web"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "gradio>=4.36",
    "httpx>=0.27",
]
